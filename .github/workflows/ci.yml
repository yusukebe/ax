name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - name: nix/bun.nix must be in sync with bun.lock
        # bun install's postinstall regenerates it; a diff means the committed
        # file is stale.
        run: git diff --exit-code nix/bun.nix
      - run: bunx tsc --noEmit
      - run: bunx oxfmt --check .
      - run: bun test
      - run: bun build src/index.ts --compile --outfile ax && ./ax --version
